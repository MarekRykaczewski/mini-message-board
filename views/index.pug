extends layout

block content
  h1= title
  p Welcome to #{title}

    for message in messages 
      div
        p= message.user
        p= message.text
        p= message.added
        
    a(href='/new') New message